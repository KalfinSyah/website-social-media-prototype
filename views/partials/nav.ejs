<%
    function isActive(page) {
        return title.toLowerCase() === page ? "fw-bold active" : "";
    }
%>

<nav class="navbar navbar-expand-lg bg-primary shadow-lg rounded-bottom-4 position-fixed w-100 top-0 start-0" style="z-index: 1050;">
    <div class="container d-flex justify-content-center">
        <button class="navbar-toggler border-0 shadow-sm" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-white px-4 py-2 rounded-pill <%= isActive('home') %>" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white px-4 py-2 rounded-pill <%= isActive('posts') %>" href="/posts">Posts</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white px-4 py-2 rounded-pill <%= isActive('users') %>" href="/users">Users</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div style="height: 70px;"></div>
